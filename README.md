# K-Nearest-Neighbors-KNN-Classification-ELEVATE-LABS.
KNN classifier on Iris: load and normalize data, split train/test, grid-search K (1–20) evaluating accuracy and cross-validation, show accuracy vs K, confusion matrix for best K, and visualize 2D decision boundaries. Runnable Python script (one file)
